name: smart-grading_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - google-adk
      - google-genai
      - python-dotenv
      - sqlalchemy
      - streamlit>=1.52.0
      - litellm
      - pytest
